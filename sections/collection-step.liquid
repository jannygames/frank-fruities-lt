{% render 'takestep', title: section.settings.title, subtitle: section.settings.subtitle %}

{% schema %}
{
  "name": "Collection: Step",
  "tag": "section",
  "limit": 1,
  "settings": [
    {
      "type": "text",
      "id": "title",
      "label": "Title"
    },
    {
      "type": "text",
      "id": "subtitle",
      "label": "Subtitle"
    }
  ]
}
{% endschema %}
